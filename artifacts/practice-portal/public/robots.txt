# Private beta — nothing here should be indexed.
#
# This is a closed feedback beta running against real chamber data. Search
# engines have no business in it, and a matter title turning up in a search
# result would be a confidentiality incident, not an SEO problem.
#
# Remove this file (or replace it with an allow rule) when the product is
# genuinely public.
User-agent: *
Disallow: /
